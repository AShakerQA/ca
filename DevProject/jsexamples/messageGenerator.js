const randommiser = num => {
    return Math.floor(Math.random() * num)
};

const Mood = {
    weather : ['sunny', 'cold', 'raining', 'windy'],
    feeling: ['good', 'slow', 'bad', 'awesome', 'dark']
};
